<?xml version="1.0" encoding="utf-8"?>
<!-- Allows the user-configured private-LAN HTTP desktop API. Flutter requires an explicit risk confirmation before credentials are sent over HTTP. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>