# Generated payment bridge

Run after changing `pigeons/payment_browser_api.dart`:

```powershell
E:\projs\KiroLucker\.tooling\flutter\bin\dart.bat run pigeon --input pigeons/payment_browser_api.dart
```

The generated Dart/Kotlin/Swift files are committed because the native app build must not depend on a code-generation step at installation time. The source definition is authoritative.
